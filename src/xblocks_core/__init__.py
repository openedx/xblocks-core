"""
Shared utilities across xblocks-core.

This module is kept intentionally minimal so that the individual XBlocks can remain
as independent as possible. If you need to share code between the XBlocks in xblocks-core,
consider upstreaming that code to the xblock package itself rather than putting it here.

This module also holds the canonical xblocks-core version string.
"""

__version__ = "1.0.0"
